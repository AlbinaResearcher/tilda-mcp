# Не загружать в GitHub (содержат секретные ключи)
.env
.env.local

# Локальная конфигурация с реальными ключами
# .mcp.json — этот файл содержит ЗАГЛУШКИ, не реальные ключи — OK загружать

# Node.js
node_modules/

# OS
.DS_Store
Thumbs.db
